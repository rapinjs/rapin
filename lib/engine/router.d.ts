export default class Router {
    private app;
    private registry;
    constructor();
    start(): Promise<void>;
    private initRegistry;
    private preRequest;
    private postRequest;
    private handleError;
}
