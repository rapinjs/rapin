export default class Router {
    private app;
    private registry;
    constructor();
    start(): void;
    private initRegistry;
    private preRequest;
    private postRequest;
    private handleError;
}
